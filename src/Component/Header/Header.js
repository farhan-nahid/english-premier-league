import React from 'react';
import './Header.css';

const Header = () => {
    return (
        <header>
          {/* banner part add in header.css */}
        </header>
    );
};

export default Header;